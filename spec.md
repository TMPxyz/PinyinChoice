# 拼音学习游戏（V1）开发规格说明

## 一、项目简介

开发一个纯前端网页小游戏，用于帮助用户学习常用汉字及拼音。

游戏通过选择题的形式进行练习，支持多种出题模式，并自动记录最高分。

整个项目无需后端，可直接部署到 GitHub Pages。

---

# 二、技术要求

## 技术栈

* HTML5
* CSS3
* JavaScript（ES6+）
* 不使用 React、Vue 等大型框架
* 可使用轻量级拼音库（如 pinyin-pro）生成数据
* 使用 Web Audio API（OfflineAudioContext）程序化生成音效和 BGM

项目应保持简单、易维护。

---

## 项目结构

```text
/
│
├── index.html
├── style.css
├── game.js
├── generateCharPY.js
├── audio.js
│
├── comchar2.txt
└── charPY.txt
```

---

# 三、数据生成

## 输入文件

根目录提供：

```text
comchar2.txt
```

文件中包含所有游戏中用到的汉字。

---

## 数据生成脚本

编写：

```text
generateCharPY.js
```

读取：

```text
comchar2.txt
```

生成：

```text
charPY.txt
```

格式：

```text
汉字,拼音1,拼音2,...
```

例如：

```text
你,ni
好,hao
长,chang,zhang
重,zhong,chong
行,xing,hang
```

要求：

* 拼音全部小写
* 不带声调
* 去重
* UTF-8 编码
* 多音字必须保留全部合法拼音
* 如果拼音库无法获得全部拼音，可自行补充字典

---

# 四、数据加载

游戏启动时读取：

```text
charPY.txt
```

加载为内存对象：

```javascript
[
    {
        char: "长",
        pinyins: ["chang", "zhang"]
    },
    ...
]
```

加载完成后，需要预先建立三个索引：

## 1、charToPinyins

```javascript
Map<char, string[]>
```

例如：

```text
长

↓

["chang","zhang"]
```

---

## 2、pinyinToChars

```javascript
Map<pinyin, char[]>
```

例如：

```text
chang

↓

["长","常","唱","厂"...]
```

---

## 3、allPinyins

所有出现过的不重复拼音组成数组，用于随机生成错误选项。

例如：

```text
[
"ni",
"hao",
"chang",
"zhang",
"zhong",
...
]
```

---

# 五、游戏流程

开始游戏：

初始化：

```text
HP = 5

Score = 0
```

读取最高分：

```text
Best Score
```

进入循环：

```text
随机模式

↓

生成题目

↓

等待回答

↓

播放回答音效

↓

答对时触发两侧彩带拉炮效果

↓

等待延迟（正确1s / 错误5s）

↓

进入下一题
```

直到：

```text
HP = 0
```

进入：

```text
Game Over
```

显示：

* Score
* Best Score
* Restart

Restart 后重新初始化游戏。

---

# 六、UI

顶部：

```text
HP ♥♥♥♥♥

Score

Best Score
```

中央：

```text
题目
```

底部：

四个按钮：

```text
○

○

○

○
```

回答后立即播放音效，延迟后自动进入下一题。

* 答对：等待 1s 后自动进入下一题
* 答错：等待 5s 后自动进入下一题

无需点击下一步。

## BGM 控制

页面右上角提供 BGM 开关按钮：

```text
🔊 / 🔇
```

* 默认自动播放 BGM
* 可随时开关
* BGM 要求 30 秒以上，支持无缝循环
* 风格适合学习环境（轻柔、舒缓）

---

# 七、游戏规则

答对：

```text
Score += 1
```

触发两侧彩带拉炮效果。

答错：

```text
HP -= 1
```

HP 为 0：

Game Over。

## 回答延迟

* 答对后：播放正确音效，**两侧发射彩带拉炮效果**，等待 **1 秒** 后自动进入下一题
* 答错后：播放错误音效，等待 **5 秒** 后自动进入下一题

延迟期间，按钮保持禁用状态，防止误触。

延迟结束后，立即生成并显示下一题。

---

# 八、最高分

使用：

```text
localStorage
```

保存：

```text
BestScore
```

启动时读取。

如果本次分数更高，则更新。

---

# 九、音效与背景音乐（BGM）

## 实现方式

使用 **Web Audio API**（`OfflineAudioContext`）程序化生成所有音频数据：

* 不需要外部音频文件
* 通过 `AudioBufferSourceNode.loop = true` 实现无缝循环
* 所有音效在游戏启动时一次性渲染完成

## BGM 要求

* 时长 30 秒以上（~29.5s，通过 `loop` 无缝衔接）
* 支持无缝循环（`AudioBufferSourceNode.loop = true`）
* **风格**：欢快、活泼（参考超级玛丽风格），适合学习时的轻松氛围
* **速度**：130 BPM（快板）
* **音色**：方波主旋律（8-bit/chiptune 风格）+ 方波贝斯 + 三角波和弦伴奏
* **调式**：C 大调，和弦进行 `C → G → Am → F`（经典流行走向）
* **结构**：4 遍变奏，每遍 16 拍，包含上行琶音、跳跃、十六分音符律动等
* 默认自动播放
* 用户可随时通过界面按钮开关 BGM

## 音效要求

* 答对音效：上行双音 "叮咚"（G5 → C6 + 高八度泛音，正弦波）
  * 时长约 0.45s，清脆悦耳
* 答错音效：低沉短促嗡嗡声（150Hz + 168Hz 不和谐二度，锯齿波）
  * 时长约 0.50s，中性不带负面情绪
* 两种音效应互不相同，让玩家能通过听觉区分结果
* 音效在 1 秒内播放完毕，不影响延迟计时

## 音频上下文

* 使用 `AudioContext` 懒初始化（首次用户交互时创建）
* 若浏览器自动播放策略阻止 BGM，在用户首次点击选项时触发 `ctx.resume()`

---

# 十、出题模式

每完成一道题后：

随机选择下一题模式。

目前包含：

* 模式1：汉字 → 拼音
* 模式2：拼音 → 汉字

两个模式概率相同。

后续应便于继续扩展新的模式。

---

# 十一、模式1：汉字 → 拼音

随机选择一个汉字。

例如：

```text
长
```

如果是多音字：

```text
chang
zhang
```

随机选择其中一个作为正确答案。

例如：

```text
正确答案：

chang
```

随后随机生成三个错误拼音。

四个按钮随机排列。

例如：

```text
长

○ chang

○ shi

○ ming

○ yun
```

---

## 模式1规则（重要）

对于：

```text
长

↓

chang

zhang
```

如果正确答案选择：

```text
chang
```

则：

```text
zhang
```

绝不能作为错误选项。

即：

错误拼音必须满足：

```text
candidate ∉ 当前汉字全部拼音
```

例如：

禁止：

```text
chang

zhang
```

只能从其它拼音中随机选择错误答案。

保证：

**整道题始终只有一个正确答案。**

---

# 十二、模式2：拼音 → 汉字

随机选择一个：

```text
长

↓

随机选择拼音

↓

chang
```

题目：

```text
chang
```

正确答案：

```text
长
```

随后生成三个错误汉字。

例如：

```text
chang

○ 长

○ 河

○ 猫

○ 云
```

四个按钮随机排列。

---

## 模式2规则（重要）

错误汉字必须满足：

其合法拼音列表中不能包含题目拼音。

例如：

题目：

```text
chang
```

则：

下面全部禁止：

```text
长

常

唱

场

厂

昌
```

因为它们都拥有：

```text
chang
```

作为合法拼音。

错误答案必须满足：

```text
题目拼音 ∉ 错误汉字全部拼音
```

保证：

**玩家永远只能找到唯一正确答案。**

---

# 十三、随机规则

所有随机使用：

```javascript
Math.random()
```

需要随机：

* 出题模式
* 汉字
* 多音字选择哪个拼音
* 四个按钮顺序
* 错误选项

---

# 十四、Game Over

HP 为 0：

显示：

```text
Game Over

Score

Best Score

Restart
```

点击：

```text
Restart
```

重新开始游戏。

---

# 十五、代码结构

建议划分：

## DataManager

负责：

* 加载 charPY.txt
* 建立索引
* 提供查询接口

---

## QuestionGenerator

负责：

* 模式1出题
* 模式2出题
* 生成错误选项
* 保证题目唯一正确答案

---

## GameManager

负责：

* HP
* Score
* Best Score
* Game Over
* localStorage

---

## UIManager

负责：

* 更新页面
* 更新按钮
* 更新分数
* 更新 HP
* 显示 Game Over

---

## AudioManager

负责：

* 使用 Web Audio API（OfflineAudioContext）程序化生成 BGM 和音效
* 播放/暂停 BGM
* 播放正确/错误音效
* BGM 无缝循环控制
* 处理浏览器自动播放策略
* 无需外部音频文件

---

# 十六、代码质量要求

* 模块职责清晰，避免所有逻辑集中在一个文件。
* 尽量减少重复代码。
* 所有主要函数应添加必要注释。
* 常量（如初始 HP、选项数量等）集中管理，便于后续调整。
* 后续新增出题模式时，不需要修改已有模式逻辑，仅需新增对应的题目生成器。

---

# 十七、验收标准

## 数据

* 能正确读取 `charPY.txt`
* 多音字全部保留
* 数据加载无报错

## 游戏

* 可以正常开始游戏
* 可以无限出题
* HP 正确减少
* Score 正确增加
* Game Over 正常触发
* Restart 正常重开

## 模式1

* 汉字显示正确
* 四个拼音选项正确生成
* 多音字不会把其它合法拼音作为错误选项
* 四个选项无重复

## 模式2

* 拼音显示正确
* 四个汉字选项正确生成
* 所有包含题目拼音的汉字都不会作为错误选项
* 四个选项无重复

## 最高分

* 正确保存到 localStorage
* 刷新页面后仍能保留

## 音效与 BGM

* BGM 可正常播放、循环无间隙
* BGM 开关按钮正常工作
* 答对时播放正确音效
* 答错时播放错误音效
* Web Audio API 初始化无报错
* 无需外部音频文件

## 回答延迟

* 答对后等待 1s 自动进入下一题
* 答错后等待 5s 自动进入下一题
* 延迟期间按钮处于禁用状态，不可点击

## 唯一正确答案

任何情况下：

* 一道题只能存在一个正确答案。
* 不允许因为多音字、同音字或数据重复导致多个正确答案。
* 任意四个选项均应保证逻辑上不存在歧义。
